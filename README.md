 1. Typescript enforces type safety at compile time by requiring the type declartion of variables and objects.

 2. Inheritence reduces code duplication by allowing child classes/objects to use its parents properties and method. 

3. The benefits of using encapsulatioin includes having control of data access by allowing what can be accessed by whom and where. 

4. If a new product needed to be added to the project, using polymorphism would make this task straitforward by defining an interface and each type of Product class would just need to implement the interface and provide its own implementation. 